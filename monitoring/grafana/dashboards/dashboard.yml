apiVersion: 1

providers:
  - name: 'default'
    folder: ''
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards

alerting:
  - name: 'warehouse-alerts'
    type: file
    options:
      path: /etc/grafana/provisioning/alerting