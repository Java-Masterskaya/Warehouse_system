apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: false
    jsonData:
      derivedFields:
        - name: traceId
          matcherRegex: "traceId\\\":\\\"(\\w+)\\\""
          url: "$${__value.raw}"
          datasourceUid: tempo