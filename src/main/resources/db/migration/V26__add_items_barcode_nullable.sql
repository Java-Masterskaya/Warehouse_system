-- ============================================================================
-- OPS-5 V26, Шаг 1: Расширение (Expand)
-- ============================================================================
-- Описание: Добавляем колонку barcode в таблицу items как nullable.
-- Безопасность: Добавление nullable-колонки без DEFAULT — это O(1) в PostgreSQL,
--               таблица не переписывается и не блокируется эксклюзивно.
-- Совместимость:
--   - Старый код (не знает про barcode) продолжает INSERT/UPDATE без ошибок.
--   - Новый код уже может писать barcode.
-- НЕ ДОБАВЛЯТЬ:
--   - NOT NULL здесь (упадёт на существующих строках).
--   - DEFAULT здесь (вызовет полное переписывание таблицы и удержание
--     ACCESS EXCLUSIVE блокировки на время выполнения для больших таблиц).
-- ============================================================================

ALTER TABLE items ADD COLUMN barcode VARCHAR(255);
